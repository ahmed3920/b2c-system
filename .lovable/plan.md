# Rename browser tab title to "B2C Management System"

## What to change
- Update `index.html` `<title>` from "iSchool Task Tracker" to "B2C Management System".
- Update matching `<meta property="og:title">` and `<meta name="twitter:title">` in `index.html` to the same text so social previews stay consistent.

## Files to edit
- `index.html`

## Outcome
The browser tab and link previews will display "B2C Management System" instead of "iSchool Task Tracker".
